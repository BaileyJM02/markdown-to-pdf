version: '3.7'

services:
  markdown-to-pdf:
    container_name: markdown-to-pdf
    build:
      context: .
      dockerfile: Dockerfile
    image: "docker.pkg.github.com/twohorus/markdown-to-pdf/markdown-to-pdf:${TAG:-latest}"
