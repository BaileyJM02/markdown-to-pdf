on:
  push:
    branches:
      - master
  pull_request:
  
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: docker build . -t markdown-to-pdf-from-ubuntu-latest
